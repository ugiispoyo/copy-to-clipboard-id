{
  "name": "copy-to-clipboard-id",
  "version": "0.0.9",
  "description": "Simple copy function to clipboard",
  "main": "dist/copy-to-clipboard-id.min.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && rollup -c && uglifyjs dist/index.js --output dist/copy-to-clipboard-id.min.js && rm dist/index.js"
  },
  "author": "Ugi Ispoyo Widodo",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-typescript": "^9.0.1",
    "@types/node": "^18.8.4",
    "rollup": "^2.79.1",
    "tslib": "^2.4.0",
    "typescript": "^4.9.3",
    "uglify-js": "^3.17.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ugiispoyo/copy-to-clipboard-id.git"
  },
  "keywords": [
    "copy to clipboard",
    "copy to clipboard id",
    "copy to clipboard js",
    "copy"
  ]
}
